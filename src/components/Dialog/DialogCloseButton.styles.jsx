import styled from 'styled-components';

const CloseButton = styled.button`
	position: absolute;
	top: 16px;
	right: 25px;
	cursor: pointer;

	svg {
		width: 100%;
		height: 100%;
	}
`;

export default CloseButton;
